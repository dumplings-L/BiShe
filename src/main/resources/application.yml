server:
  port: 8088

spring:
  mvc:
    view:
      #“/”表示resources/static/
      prefix: /
      suffix: .html
  thymeleaf:
    cache: false
    #默认目录是resources/templates/
    #prefix: classpath:/myPage/
